name: Greeting from Mona
on: push

env: 
  TZ: Asia/Shanghai
  DOUYIN-COOKIES: ${{ secrets.test }}
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps: 
      - name: checkout actions
        uses: actions/checkout@v3

      - name: set up python 3.9
        uses: actions/setup-python@v4
        with: 
          python-version : '3.9'

      # - name: set up ffmpeg
      #   uses: FedericoCarboni/setup-ffmpeg@v2

      - name: set up ffmpeg
        run: |
          sudo apt update
          sudo apt install ffmpeg


      - name: set up node 14.8
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: running
        run: |
          pip install aligo

          git config --local user.email "git@qq.com"
          git config --local user.name "Ezong"
          mkdir -p ~/.aligo/
          cat >> ~/.aligo/aligo.json << EOF
          ${{ secrets.test }}
          EOF
          ffmpeg -i "http://open-tct.douyucdn2.cn/dyliveflv3a/115280rZMKulyF7Q_2000.flv?wsAuth=9155c8b109f4c32236cbfcaaf2f49406&token=cpn-androidmpro-0-115280-580dca762c3607264118cbc28271899d1c514268ca9df17e&logo=0&expire=0&did=a6b66c7c81a583ee867932bbc99f238a&origin=tct&vhost=play4&sid=351418660" -c:a copy -c:v copy a.mp4
          python aa.py
          

      #     git commit -a -m "Add changes"
      # - name: Push changes
      #   uses: ad-m/github-push-action@master
      #   with:
      #     github_token: ${{ secrets.github_token }}
      #     branch: ${{ github.ref }}