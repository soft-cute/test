name: Greeting from Mona
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps: 
      - name: Print a greeting
        run: |
          ls -a
          echo ${{ secrets.test }}
          curl http://ip.3322.net
