name: Greeting from Mona
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - name: Print a greeting
        run: |
          git -v
          ls -a
          echo ${{ secrets.test }}
